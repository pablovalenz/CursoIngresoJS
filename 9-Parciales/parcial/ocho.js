function Mostrar()
{
    var contador = 0;
    var respuesta = "continuar";
    var temperatura;

       
    while (respuesta != "no") 
        {
            temperatura = prompt ("Ingrese una temperatura");
            temperatura = parseInt (temperatura)
            while (temperatura < -100 || temperatura >100 || isNaN (temperatura) ) {//Inicio llave while
                temperatura = prompt ()
                temperatura = parseInt

            }
            
            
            
            
            
            
            respuesta = prompt ("Ingrese no para salir");
            
        }
    //Fin del while
alert = h


}
